# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Project-local glide cache, RE: https://github.com/Masterminds/glide/issues/736
.glide/

# IntelliJ
.idea
*iml

# vendor folder created by go dep
vendor/

# tests covers files
*.coverprofile

# lets add Gopkg.lock to .gitignore to ensure having latest version of dependencies during CI builds
Gopkg.lock

.DS_Store
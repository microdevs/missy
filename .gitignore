# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# IntelliJ
.idea
*iml

# vendor folder created by go dep
vendor/

# tests covers files
*.coverprofile

# root go mod because each package is it's own module
go.mod